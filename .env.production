# Production Environment Configuration
# IMPORTANT: Keep this file secure and never commit to version control

# Database Configuration (Production)
DB_NAME=tlng_summit_prod
DB_USER=tlng_summit_user
DB_PASSWORD=your_secure_database_password_here
DB_HOST=localhost
DB_PORT=5432

# Django Secret Key - MUST be changed for production
SECRET_KEY=your-super-secret-production-key-here-change-this-immediately

# Email Configuration (Production SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your_email@transportandlogisticssummit.ng
EMAIL_HOST_PASSWORD=your_email_password_or_app_password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@transportandlogisticssummit.ng
ADMIN_EMAIL=admin@transportandlogisticssummit.ng

# Domain Configuration (Production)
ALLOWED_HOSTS=transportandlogisticssummit.ng,www.transportandlogisticssummit.ng

# Debug Mode (MUST be False for production)
DEBUG=False

# Optional: Additional security settings
SECURE_SSL_REDIRECT=True
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_TYPE_NOSNIFF=True
X_FRAME_OPTIONS=DENY